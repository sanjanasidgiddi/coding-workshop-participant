import IncidentTable from '../../components/IncidentTable'

export default function MyAssignedTicketsPage() {
  return <IncidentTable title="My Assigned Tickets" emptyMessage="You have no assigned tickets." />
}
